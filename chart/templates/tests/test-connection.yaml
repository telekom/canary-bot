apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canary-bot.fullname" . }}-test-connection"
  labels:
    {{- include "canary-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canary-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
